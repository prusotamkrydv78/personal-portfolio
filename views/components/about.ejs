<section id="about" class="relative py-24 overflow-hidden">
  <!-- Ultra-Modern Futuristic Background Elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <!-- Dynamic Gradient Background -->
    <div class="absolute inset-0 ultramodern-bg">
      <div class="ultramodern-gradient"></div>
      <div class="ultramodern-noise"></div>
    </div>

    <!-- Quantum Grid Pattern -->
    <div class="absolute inset-0 quantum-grid">
      <div class="quantum-grid-horizontal"></div>
      <div class="quantum-grid-vertical"></div>
      <div class="quantum-grid-diagonal"></div>
    </div>

    <!-- Advanced Particle System -->
    <div
      class="absolute inset-0 quantum-particles"
      id="quantum-particles"
    ></div>

    <!-- Neural Network Visualization -->
    <div class="absolute inset-0 neural-network">
      <svg
        class="neural-network-svg"
        width="100%"
        height="100%"
        viewBox="0 0 100 100"
        preserveAspectRatio="none"
      >
        <g class="neural-nodes">
          <circle class="neural-node" cx="20" cy="20" r="0.5" />
          <circle class="neural-node" cx="40" cy="30" r="0.5" />
          <circle class="neural-node" cx="60" cy="15" r="0.5" />
          <circle class="neural-node" cx="80" cy="25" r="0.5" />
          <circle class="neural-node" cx="30" cy="50" r="0.5" />
          <circle class="neural-node" cx="70" cy="60" r="0.5" />
          <circle class="neural-node" cx="20" cy="70" r="0.5" />
          <circle class="neural-node" cx="50" cy="80" r="0.5" />
          <circle class="neural-node" cx="80" cy="75" r="0.5" />
        </g>
        <g class="neural-connections">
          <path class="neural-connection" d="M20,20 L40,30" />
          <path class="neural-connection" d="M40,30 L60,15" />
          <path class="neural-connection" d="M60,15 L80,25" />
          <path class="neural-connection" d="M30,50 L40,30" />
          <path class="neural-connection" d="M30,50 L70,60" />
          <path class="neural-connection" d="M20,70 L30,50" />
          <path class="neural-connection" d="M50,80 L70,60" />
          <path class="neural-connection" d="M80,75 L70,60" />
          <path class="neural-connection" d="M50,80 L20,70" />
        </g>
      </svg>
    </div>

    <!-- Quantum Decorative Elements -->
    <div class="absolute right-10 top-20 quantum-shape">
      <div class="quantum-orb">
        <div class="quantum-orb-rings">
          <div class="quantum-orb-ring"></div>
          <div class="quantum-orb-ring"></div>
          <div class="quantum-orb-ring"></div>
        </div>
        <div class="quantum-orb-core"></div>
        <div class="quantum-orb-glow"></div>
      </div>
    </div>

    <div class="absolute left-10 bottom-20 quantum-shape">
      <div class="quantum-cube">
        <div class="quantum-cube-face quantum-cube-front"></div>
        <div class="quantum-cube-face quantum-cube-back"></div>
        <div class="quantum-cube-face quantum-cube-top"></div>
        <div class="quantum-cube-face quantum-cube-bottom"></div>
        <div class="quantum-cube-face quantum-cube-left"></div>
        <div class="quantum-cube-face quantum-cube-right"></div>
        <div class="quantum-cube-glow"></div>
      </div>
    </div>

    <!-- Data Stream Animation -->
    <div class="absolute inset-0 data-stream-container">
      <div
        class="data-stream"
        style="--delay: 0s; --top: 15%; --speed: 15s"
      ></div>
      <div
        class="data-stream"
        style="--delay: 2s; --top: 35%; --speed: 12s"
      ></div>
      <div
        class="data-stream"
        style="--delay: 1s; --top: 55%; --speed: 18s"
      ></div>
      <div
        class="data-stream"
        style="--delay: 3s; --top: 75%; --speed: 10s"
      ></div>
      <div
        class="data-stream"
        style="--delay: 0.5s; --top: 90%; --speed: 20s"
      ></div>
    </div>
  </div>

  <div class="container mx-auto px-6 relative z-10">
    <!-- Ultra-Modern Section Title -->
    <div class="text-center mb-16">
      <div class="quantum-badge reveal">
        <div class="quantum-badge-border"></div>
        <div class="quantum-badge-glow"></div>
        <div class="quantum-badge-content">
          <span class="quantum-badge-text">ABOUT ME</span>
          <div class="quantum-badge-icon">
            <i class="fas fa-user-astronaut"></i>
          </div>
        </div>
      </div>

      <h2
        class="section-title text-4xl md:text-6xl font-bold font-display mb-6 inline-block relative reveal"
      >
        <div class="quantum-heading">
          <span class="quantum-heading-text"
            >Know Who <span class="quantum-gradient-text">I Am</span></span
          >
          <div class="quantum-heading-decoration">
            <div class="quantum-heading-line"></div>
            <div class="quantum-heading-dots">
              <div class="quantum-heading-dot"></div>
              <div class="quantum-heading-dot"></div>
              <div class="quantum-heading-dot"></div>
            </div>
          </div>
          <div class="quantum-heading-glow"></div>
        </div>
      </h2>

      <p class="text-gray-400 max-w-2xl mx-auto reveal quantum-text">
        Get to know more about me, my background, and what drives my passion for
        creating cutting-edge web experiences.
      </p>
    </div>

    <div class="quantum-content-layout">
      <!-- Quantum Holographic Profile Display -->
      <div class="quantum-profile-container order-2 lg:order-1">
        <div class="quantum-profile-display">
          <!-- Quantum Frame Elements -->
          <div class="quantum-frame">
            <div class="quantum-frame-border"></div>
            <div class="quantum-frame-corner quantum-frame-corner-tl"></div>
            <div class="quantum-frame-corner quantum-frame-corner-tr"></div>
            <div class="quantum-frame-corner quantum-frame-corner-bl"></div>
            <div class="quantum-frame-corner quantum-frame-corner-br"></div>

            <!-- Quantum Scanning Effects -->
            <div class="quantum-scan-line quantum-scan-horizontal"></div>
            <div class="quantum-scan-line quantum-scan-vertical"></div>

            <!-- Quantum Data Points -->
            <div class="quantum-data-points">
              <div
                class="quantum-data-point"
                style="--index: 1; --top: 15%; --left: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 2; --top: 35%; --left: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 3; --top: 55%; --left: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 4; --top: 75%; --left: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 5; --top: 15%; --right: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 6; --top: 35%; --right: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 7; --top: 55%; --right: -5px"
              ></div>
              <div
                class="quantum-data-point"
                style="--index: 8; --top: 75%; --right: -5px"
              ></div>
            </div>
          </div>

          <!-- Advanced Holographic Container -->
          <div class="quantum-hologram">
            <!-- Quantum Projection Rings -->
            <div class="quantum-projection-rings">
              <div class="quantum-projection-ring"></div>
              <div
                class="quantum-projection-ring"
                style="animation-delay: 0.5s"
              ></div>
              <div
                class="quantum-projection-ring"
                style="animation-delay: 1s"
              ></div>
            </div>

            <!-- Profile Image with Quantum Effects -->
            <div
              class="quantum-profile-image tilt"
              data-tilt
              data-tilt-max="10"
              data-tilt-speed="400"
              data-tilt-perspective="1000"
              data-tilt-glare
              data-tilt-max-glare="0.4"
            >
              <!-- Quantum Overlay Effects -->
              <div class="quantum-overlay"></div>
              <div class="quantum-scanlines"></div>

              <!-- Advanced Glitch Effects -->
              <div class="quantum-glitch-container">
                <div
                  class="quantum-glitch-layer"
                  style="--offset: 3px; --blend: screen"
                ></div>
                <div
                  class="quantum-glitch-layer"
                  style="--offset: -3px; --blend: multiply"
                ></div>
                <div
                  class="quantum-glitch-layer"
                  style="--offset: 1px; --blend: overlay"
                ></div>
              </div>

              <!-- Profile Image -->
              <div class="quantum-image-wrapper">
                <img
                  src="/images/about.png"
                  alt="About Me"
                  class="quantum-image"
                />
              </div>

              <!-- Quantum Particle Effects -->
              <div class="quantum-particles-overlay"></div>
            </div>

            <!-- Quantum Energy Field -->
            <div class="quantum-energy-field">
              <div class="quantum-energy-wave"></div>
              <div
                class="quantum-energy-wave"
                style="animation-delay: 0.5s"
              ></div>
              <div
                class="quantum-energy-wave"
                style="animation-delay: 1s"
              ></div>
            </div>
          </div>
        </div>

        <!-- Quantum Tech Stack Card -->
        <div class="quantum-floating-card quantum-tech-card reveal">
          <div class="quantum-card-border"></div>
          <div class="quantum-card-glow"></div>
          <div class="quantum-card-content">
            <div class="quantum-tech-icons">
              <div class="quantum-tech-icon">
                <div class="quantum-tech-icon-bg"></div>
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="currentColor"
                  width="24"
                  height="24"
                >
                  <path
                    d="M11.572 0c-.176 0-.31.001-.358.007a19.76 19.76 0 0 1-.364.033C7.443.346 4.25 2.185 2.228 5.012a11.875 11.875 0 0 0-2.119 5.243c-.096.659-.108.854-.108 1.747s.012 1.089.108 1.748c.652 4.506 3.86 8.292 8.209 9.695.779.25 1.6.422 2.534.525.363.04 1.935.04 2.299 0 1.611-.178 2.977-.577 4.323-1.264.207-.106.247-.134.219-.158-.02-.013-.9-1.193-1.955-2.62l-1.919-2.592-2.404-3.558a338.739 338.739 0 0 0-2.422-3.556c-.009-.002-.018 1.579-.023 3.51-.007 3.38-.01 3.515-.052 3.595a.426.426 0 0 1-.206.214c-.075.037-.14.044-.495.044H7.81l-.108-.068a.438.438 0 0 1-.157-.171l-.05-.106.006-4.703.007-4.705.072-.092a.645.645 0 0 1 .174-.143c.096-.047.134-.051.54-.051.478 0 .558.018.682.154.035.038 1.337 1.999 2.895 4.361a10760.433 10760.433 0 0 0 4.735 7.17l1.9 2.879.096-.063a12.317 12.317 0 0 0 2.466-2.163 11.944 11.944 0 0 0 2.824-6.134c.096-.66.108-.854.108-1.748 0-.893-.012-1.088-.108-1.747-.652-4.506-3.859-8.292-8.208-9.695a12.597 12.597 0 0 0-2.499-.523A33.119 33.119 0 0 0 11.573 0zm4.069 7.217c.347 0 .408.005.486.047a.473.473 0 0 1 .237.277c.018.06.023 1.365.018 4.304l-.006 4.218-.744-1.14-.746-1.14v-3.066c0-1.982.01-3.097.023-3.15a.478.478 0 0 1 .233-.296c.096-.05.13-.054.5-.054z"
                  />
                </svg>
              </div>
              <div class="quantum-tech-icon">
                <div class="quantum-tech-icon-bg"></div>
                <i class="fab fa-react"></i>
              </div>
              <div class="quantum-tech-icon">
                <div class="quantum-tech-icon-bg"></div>
                <i class="fab fa-angular"></i>
              </div>
              <div class="quantum-tech-icon">
                <div class="quantum-tech-icon-bg"></div>
                <i class="fab fa-node-js"></i>
              </div>
              <div class="quantum-tech-icon">
                <div class="quantum-tech-icon-bg"></div>
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="24" height="24">
                  <path d="M17.193 9.555c-1.264-5.58-4.252-7.414-4.573-8.115-.28-.394-.53-.954-.735-1.44-.036.495-.055.685-.523 1.184-.723.566-4.438 3.682-4.74 10.02-.282 5.912 4.27 9.435 4.888 9.884l.07.05A73.49 73.49 0 0111.91 24h.481c.114-1.032.284-2.056.51-3.07.417-.296.604-.463.85-.693a11.342 11.342 0 003.639-8.464c.01-.814-.103-1.662-.197-2.218zm-5.336 8.195s0-8.291.275-8.29c.213 0 .49 10.695.49 10.695-.381-.045-.765-1.76-.765-2.405z"/>
                </svg>
              </div>
            </div>
            <div class="quantum-card-text">
              <div class="quantum-card-label">Working with</div>
              <div class="quantum-card-value">Modern Tech Stack</div>
            </div>
          </div>
          <div class="quantum-card-decoration">
            <div class="quantum-card-dots">
              <div class="quantum-card-dot"></div>
              <div class="quantum-card-dot"></div>
              <div class="quantum-card-dot"></div>
            </div>
          </div>
        </div>

        <!-- Quantum Code Terminal -->
        <div class="quantum-floating-card quantum-terminal-card reveal">
          <div class="quantum-terminal-header">
            <div class="quantum-terminal-controls">
              <span class="quantum-terminal-control"></span>
              <span class="quantum-terminal-control"></span>
              <span class="quantum-terminal-control"></span>
            </div>
            <div class="quantum-terminal-title">My Ultimate Thinking</div>
            <div class="quantum-terminal-actions">
              <span class="quantum-terminal-action"></span>
              <span class="quantum-terminal-action"></span>
            </div>
          </div>
          <div class="quantum-terminal-content">
            <div class="quantum-terminal-line">
              <span class="quantum-terminal-prompt">$</span>
              <span class="quantum-terminal-command"
                >const <span class="quantum-terminal-var">passion</span> =
                <span class="quantum-terminal-string"
                  >'FullStack Development'</span
                >;</span
              >
            </div>
            <div class="quantum-terminal-line">
              <span class="quantum-terminal-prompt">$</span>
              <span class="quantum-terminal-command"
                >function
                <span class="quantum-terminal-func">createExperience</span>()
                {</span
              >
            </div>
            <div class="quantum-terminal-line quantum-terminal-indent">
              <span class="quantum-terminal-command"
                >return
                <span class="quantum-terminal-string">'Extraordinary'</span
                >;</span
              >
            </div>
            <div class="quantum-terminal-line">
              <span class="quantum-terminal-prompt">$</span>
              <span class="quantum-terminal-command">}</span>
            </div>
            <div class="quantum-terminal-line">
              <span class="quantum-terminal-prompt">$</span>
              <span class="quantum-terminal-cursor"></span>
            </div>
          </div>
          <div class="quantum-terminal-glow"></div>
        </div>
      </div>

      <!-- Quantum Content Section -->
      <div class="quantum-content-container order-1 lg:order-2">
        <div class="quantum-section-badge reveal">
          <div class="quantum-section-badge-icon">
            <i class="fas fa-user-astronaut"></i>
          </div>
          <div class="quantum-section-badge-text">MY BACKGROUND</div>
          <div class="quantum-section-badge-decoration">
            <div class="quantum-badge-line"></div>
            <div class="quantum-badge-dot"></div>
          </div>
        </div>

        <h3 class="quantum-section-title reveal">
          Creating digital experiences with
          <span class="quantum-gradient-text">passion</span> and
          <span class="quantum-gradient-text">precision</span>
          <div class="quantum-title-decoration">
            <div class="quantum-title-line"></div>
            <div class="quantum-title-glow"></div>
          </div>
        </h3>

        <div class="quantum-text-container reveal">
          <div class="quantum-text-interface">
            <div class="quantum-text-line-numbers">
              <div class="quantum-line-number">01</div>
              <div class="quantum-line-number">02</div>
              <div class="quantum-line-number">03</div>
            </div>
            <div class="quantum-text-content">
              <p class="quantum-paragraph">
                Hello! I'm <span class="quantum-highlight">Prusotam</span>, a
                passionate web developer currently in 12th grade. I love
                creating beautiful, functional, and user-friendly websites that
                deliver exceptional user experiences.
              </p>

              <p class="quantum-paragraph">
                My journey in web development started with HTML and CSS, and
                I've since expanded my skills to include modern frameworks like
                <span class="quantum-highlight">Nextjs</span> ,
                <span class="quantum-highlight">Angular</span> ,<span
                  class="quantum-highlight"
                  >ReactjS</span
                > , <span class="quantum-highlight">NodeJS</span> ,
                <span class="quantum-highlight">MongoDB</span> and tools like
                <span class="quantum-highlight">Tailwind CSS</span> ,
                <span class="quantum-highlight">GSAP</span> , and
                <span class="quantum-highlight">Lenis</span>.
              </p>

              <p class="quantum-paragraph">
                I'm constantly learning and exploring new technologies to
                improve my skills and create better web experiences. My goal is
                to build websites that are not only visually appealing but also
                performant and accessible.
              </p>
            </div>
          </div>
          <div class="quantum-text-decoration">
            <div class="quantum-text-scan"></div>
          </div>
        </div>

        <!-- Quantum Stats Display -->
        <div class="quantum-stats-container reveal">
          <div
            class="quantum-stat-item"
            data-description="Completed projects including websites, web apps, and UI/UX designs"
          >
            <div class="quantum-stat-tooltip">
              Completed projects including websites, web apps, and UI/UX designs
            </div>
            <div class="quantum-stat-hexagon">
              <div class="quantum-stat-hexagon-bg"></div>
              <div class="quantum-stat-hexagon-border"></div>
              <div class="quantum-stat-value counter" data-target="10">0</div>
              <svg class="quantum-stat-progress" viewBox="0 0 100 100">
                <path
                  class="quantum-stat-progress-path"
                  d="M50,10 L90,30 L90,70 L50,90 L10,70 L10,30 Z"
                  style="--percent: 100"
                ></path>
              </svg>
            </div>
            <div class="quantum-stat-label">Projects</div>
          </div>
          <div
            class="quantum-stat-item"
            data-description="Modern technologies and frameworks mastered including Angular, React, and Node.js"
          >
            <div class="quantum-stat-tooltip">
              Modern technologies and frameworks mastered including Angular,
              React, and Node.js
            </div>
            <div class="quantum-stat-hexagon">
              <div class="quantum-stat-hexagon-bg"></div>
              <div class="quantum-stat-hexagon-border"></div>
              <div class="quantum-stat-value counter" data-target="12">0</div>
              <svg class="quantum-stat-progress" viewBox="0 0 100 100">
                <path
                  class="quantum-stat-progress-path"
                  d="M50,10 L90,30 L90,70 L50,90 L10,70 L10,30 Z"
                  style="--percent: 100"
                ></path>
              </svg>
            </div>
            <div class="quantum-stat-label">Technologies</div>
          </div>
          <div
            class="quantum-stat-item"
            data-description="Years of experience in web development and design"
          >
            <div class="quantum-stat-tooltip">
              Years of experience in web development and design
            </div>
            <div class="quantum-stat-hexagon">
              <div class="quantum-stat-hexagon-bg"></div>
              <div class="quantum-stat-hexagon-border"></div>
              <div class="quantum-stat-value counter" data-target="2">0</div>
              <svg class="quantum-stat-progress" viewBox="0 0 100 100">
                <path
                  class="quantum-stat-progress-path"
                  d="M50,10 L90,30 L90,70 L50,90 L10,70 L10,30 Z"
                  style="--percent: 100"
                ></path>
              </svg>
            </div>
            <div class="quantum-stat-label">Years</div>
          </div>
          <div
            class="quantum-stat-item"
            data-description="Satisfied clients with successful project deliveries"
          >
            <div class="quantum-stat-tooltip">
              Satisfied clients with successful project deliveries
            </div>
            <div class="quantum-stat-hexagon">
              <div class="quantum-stat-hexagon-bg"></div>
              <div class="quantum-stat-hexagon-border"></div>
              <div class="quantum-stat-value counter" data-target="5">0</div>
              <svg class="quantum-stat-progress" viewBox="0 0 100 100">
                <path
                  class="quantum-stat-progress-path"
                  d="M50,10 L90,30 L90,70 L50,90 L10,70 L10,30 Z"
                  style="--percent: 100"
                ></path>
              </svg>
            </div>
            <div class="quantum-stat-label">Clients</div>
          </div>
        </div>

        <!-- Quantum Service Cards -->
        <div class="quantum-services-container reveal">
          <div class="quantum-service-card">
            <div class="quantum-service-header">
              <div class="quantum-service-icon-container">
                <div class="quantum-service-icon-bg"></div>
                <div class="quantum-service-icon">
                  <i class="fas fa-code"></i>
                </div>
                <div class="quantum-service-icon-glow"></div>
              </div>
              <h4 class="quantum-service-title">Web Development</h4>
              <div class="quantum-service-tag">01</div>
            </div>
            <div class="quantum-service-content">
              <p class="quantum-service-description">
                Creating responsive and user-friendly websites using modern
                technologies.
              </p>
            </div>
            <div class="quantum-service-decoration">
              <div class="quantum-service-line"></div>
              <div class="quantum-service-dots">
                <div class="quantum-service-dot"></div>
                <div class="quantum-service-dot"></div>
                <div class="quantum-service-dot"></div>
              </div>
            </div>
            <div class="quantum-service-glow"></div>
          </div>

          <div class="quantum-service-card">
            <div class="quantum-service-header">
              <div class="quantum-service-icon-container">
                <div class="quantum-service-icon-bg"></div>
                <div class="quantum-service-icon">
                  <i class="fas fa-paint-brush"></i>
                </div>
                <div class="quantum-service-icon-glow"></div>
              </div>
              <h4 class="quantum-service-title">UI/UX Design</h4>
              <div class="quantum-service-tag">02</div>
            </div>
            <div class="quantum-service-content">
              <p class="quantum-service-description">
                Designing intuitive and visually appealing user interfaces.
              </p>
            </div>
            <div class="quantum-service-decoration">
              <div class="quantum-service-line"></div>
              <div class="quantum-service-dots">
                <div class="quantum-service-dot"></div>
                <div class="quantum-service-dot"></div>
                <div class="quantum-service-dot"></div>
              </div>
            </div>
            <div class="quantum-service-glow"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Ultra-Modern Background Elements */
  .ultramodern-bg {
    position: relative;
    overflow: hidden;
  }

  .ultramodern-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      125deg,
      rgba(10, 10, 15, 0.95),
      rgba(19, 19, 31, 0.9),
      rgba(10, 10, 15, 0.95)
    );
    animation: gradient-shift 15s ease infinite alternate;
  }

  @keyframes gradient-shift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .ultramodern-noise {
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.1'/%3E%3C/svg%3E");
    opacity: 0.15;
    mix-blend-mode: overlay;
  }

  /* Quantum Grid Pattern */
  .quantum-grid {
    position: absolute;
    inset: 0;
    opacity: 0.1;
  }

  .quantum-grid-horizontal,
  .quantum-grid-vertical,
  .quantum-grid-diagonal {
    position: absolute;
    inset: 0;
  }

  .quantum-grid-horizontal {
    background-image: linear-gradient(
      to bottom,
      transparent,
      transparent 49.9%,
      rgba(99, 102, 241, 0.2) 50%,
      transparent 50.1%
    );
    background-size: 100% 40px;
    animation: grid-pulse 8s linear infinite;
  }

  .quantum-grid-vertical {
    background-image: linear-gradient(
      to right,
      transparent,
      transparent 49.9%,
      rgba(236, 72, 153, 0.2) 50%,
      transparent 50.1%
    );
    background-size: 40px 100%;
    animation: grid-pulse 8s linear infinite reverse;
  }

  .quantum-grid-diagonal {
    background-image: repeating-linear-gradient(
      45deg,
      transparent,
      transparent 20px,
      rgba(139, 92, 246, 0.1) 20px,
      transparent 40px
    );
    opacity: 0.3;
  }

  @keyframes grid-pulse {
    0%,
    100% {
      opacity: 0.05;
    }
    50% {
      opacity: 0.2;
    }
  }

  /* Neural Network Visualization */
  .neural-network {
    position: absolute;
    inset: 0;
    opacity: 0.15;
    overflow: hidden;
  }

  .neural-network-svg {
    width: 100%;
    height: 100%;
  }

  .neural-node {
    fill: var(--color-primary);
    opacity: 0.7;
    animation: node-pulse 4s ease-in-out infinite;
  }

  .neural-connection {
    stroke: var(--color-primary);
    stroke-width: 0.2;
    opacity: 0.3;
    animation: connection-pulse 6s ease-in-out infinite;
  }

  @keyframes node-pulse {
    0%,
    100% {
      r: 0.5;
      opacity: 0.7;
    }
    50% {
      r: 1;
      opacity: 1;
    }
  }

  @keyframes connection-pulse {
    0%,
    100% {
      stroke-width: 0.2;
      opacity: 0.3;
    }
    50% {
      stroke-width: 0.5;
      opacity: 0.6;
    }
  }

  /* Data Stream Animation */
  .data-stream-container {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .data-stream {
    position: absolute;
    left: 0;
    width: 100%;
    height: 1px;
    top: var(--top);
    background: linear-gradient(
      90deg,
      transparent,
      var(--color-primary),
      transparent
    );
    opacity: 0.3;
    animation: data-stream var(--speed) linear infinite;
    animation-delay: var(--delay);
  }

  @keyframes data-stream {
    0% {
      transform: translateX(-100%);
    }
    100% {
      transform: translateX(100%);
    }
  }

  /* Quantum Decorative Elements */
  .quantum-shape {
    position: relative;
    width: 100px;
    height: 100px;
    opacity: 0.6;
  }

  /* Quantum Orb */
  .quantum-orb {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .quantum-orb-rings {
    position: absolute;
    inset: 0;
  }

  .quantum-orb-ring {
    position: absolute;
    inset: 0;
    border: 1px solid var(--color-primary);
    border-radius: 50%;
    opacity: 0.5;
    animation: quantum-ring-rotate 10s linear infinite;
  }

  .quantum-orb-ring:nth-child(2) {
    border-color: var(--color-secondary);
    animation-duration: 15s;
    animation-direction: reverse;
  }

  .quantum-orb-ring:nth-child(3) {
    border-color: var(--color-accent);
    animation-duration: 20s;
  }

  @keyframes quantum-ring-rotate {
    0% {
      transform: rotate(0deg) scale(0.8);
    }
    100% {
      transform: rotate(360deg) scale(1.2);
    }
  }

  .quantum-orb-core {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 20px;
    height: 20px;
    background: radial-gradient(circle, var(--color-primary), transparent 70%);
    border-radius: 50%;
    animation: quantum-core-pulse 4s ease-in-out infinite;
  }

  @keyframes quantum-core-pulse {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.7;
    }
    50% {
      transform: translate(-50%, -50%) scale(1.5);
      opacity: 1;
    }
  }

  .quantum-orb-glow {
    position: absolute;
    inset: -20px;
    background: radial-gradient(
      circle,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.1;
    border-radius: 50%;
    filter: blur(10px);
    animation: quantum-glow-pulse 6s ease-in-out infinite;
  }

  @keyframes quantum-glow-pulse {
    0%,
    100% {
      opacity: 0.1;
      transform: scale(1);
    }
    50% {
      opacity: 0.3;
      transform: scale(1.2);
    }
  }

  /* Quantum Cube */
  .quantum-cube {
    position: relative;
    width: 60px;
    height: 60px;
    transform-style: preserve-3d;
    transform: rotateX(-30deg) rotateY(45deg);
    animation: quantum-cube-rotate 20s linear infinite;
  }

  @keyframes quantum-cube-rotate {
    0% {
      transform: rotateX(-30deg) rotateY(0deg);
    }
    100% {
      transform: rotateX(-30deg) rotateY(360deg);
    }
  }

  .quantum-cube-face {
    position: absolute;
    width: 100%;
    height: 100%;
    border: 1px solid var(--color-secondary);
    opacity: 0.3;
    background: rgba(236, 72, 153, 0.05);
  }

  .quantum-cube-front {
    transform: translateZ(30px);
  }
  .quantum-cube-back {
    transform: translateZ(-30px) rotateY(180deg);
  }
  .quantum-cube-top {
    transform: rotateX(90deg) translateZ(30px);
  }
  .quantum-cube-bottom {
    transform: rotateX(-90deg) translateZ(30px);
  }
  .quantum-cube-left {
    transform: rotateY(-90deg) translateZ(30px);
  }
  .quantum-cube-right {
    transform: rotateY(90deg) translateZ(30px);
  }

  .quantum-cube-glow {
    position: absolute;
    inset: -10px;
    background: radial-gradient(
      circle,
      var(--color-secondary) 0%,
      transparent 70%
    );
    opacity: 0.1;
    filter: blur(10px);
    animation: quantum-glow-pulse 6s ease-in-out infinite;
  }

  /* Quantum Badge */
  .quantum-badge {
    position: relative;
    display: inline-flex;
    padding: 0.5rem 1.5rem;
    margin: 0 auto;
    background: rgba(19, 19, 31, 0.7);
    border-radius: 30px;
    overflow: hidden;
    z-index: 1;
  }

  .quantum-badge-border {
    position: absolute;
    inset: 0;
    border: 1px solid var(--color-primary);
    border-radius: 30px;
    opacity: 0.5;
    z-index: -1;
  }

  .quantum-badge-glow {
    position: absolute;
    inset: -5px;
    background: radial-gradient(
      circle at center,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.1;
    filter: blur(10px);
    animation: badge-glow-pulse 4s ease-in-out infinite;
    z-index: -1;
  }

  @keyframes badge-glow-pulse {
    0%,
    100% {
      opacity: 0.1;
    }
    50% {
      opacity: 0.3;
    }
  }

  .quantum-badge-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
  }

  .quantum-badge-text {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 2px;
    color: var(--color-primary);
    text-transform: uppercase;
  }

  .quantum-badge-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
    color: var(--color-primary);
    animation: badge-icon-pulse 3s ease-in-out infinite;
  }

  @keyframes badge-icon-pulse {
    0%,
    100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.2);
    }
  }

  /* Quantum Heading */
  .quantum-heading {
    position: relative;
    display: inline-block;
  }

  .quantum-heading-text {
    position: relative;
    z-index: 1;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-shadow: 0 0 10px rgba(99, 102, 241, 0.3);
  }

  .quantum-gradient-text {
    background: linear-gradient(
      90deg,
      var(--color-primary),
      var(--color-secondary),
      var(--color-accent)
    );
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradient-shift 8s linear infinite;
  }

  .quantum-heading-decoration {
    position: relative;
    margin-top: 0.5rem;
  }

  .quantum-heading-line {
    width: 100%;
    height: 2px;
    background: linear-gradient(
      90deg,
      transparent,
      var(--color-primary),
      var(--color-secondary),
      transparent
    );
    animation: line-scan 3s linear infinite;
  }

  @keyframes line-scan {
    0% {
      background-position: -200% 0;
    }
    100% {
      background-position: 300% 0;
    }
  }

  .quantum-heading-dots {
    position: absolute;
    top: -4px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 8px;
  }

  .quantum-heading-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-primary);
    opacity: 0.7;
    animation: dot-pulse 3s ease-in-out infinite;
  }

  .quantum-heading-dot:nth-child(2) {
    animation-delay: 0.5s;
    background: var(--color-secondary);
  }

  .quantum-heading-dot:nth-child(3) {
    animation-delay: 1s;
    background: var(--color-accent);
  }

  @keyframes dot-pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.7;
    }
    50% {
      transform: scale(1.5);
      opacity: 1;
    }
  }

  .quantum-heading-glow {
    position: absolute;
    inset: -10px;
    background: radial-gradient(
      ellipse at center,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.1;
    filter: blur(10px);
    z-index: -1;
    animation: heading-glow-pulse 6s ease-in-out infinite;
  }

  @keyframes heading-glow-pulse {
    0%,
    100% {
      opacity: 0.1;
      transform: scale(1);
    }
    50% {
      opacity: 0.2;
      transform: scale(1.1);
    }
  }

  /* Quantum Text */
  .quantum-text {
    position: relative;
    line-height: 1.7;
    color: var(--color-text-alt);
    text-shadow: 0 0 5px rgba(99, 102, 241, 0.2);
  }

  /* Quantum Content Layout */
  .quantum-content-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    margin-top: 3rem;
  }

  @media (min-width: 1024px) {
    .quantum-content-layout {
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }
  }

  /* Quantum Profile Container */
  .quantum-profile-container {
    position: relative;
  }

  .quantum-profile-display {
    position: relative;
    width: 100%;
    max-width: 500px;
    margin: 0 auto;
    aspect-ratio: 4/3;
  }

  /* Quantum Frame */
  .quantum-frame {
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
  }

  .quantum-frame-border {
    position: absolute;
    inset: 10px;
    border: 1px solid rgba(99, 102, 241, 0.3);
    border-radius: 4px;
  }

  .quantum-frame-corner {
    position: absolute;
    width: 25px;
    height: 25px;
    border: 2px solid var(--color-primary);
    z-index: 5;
    transition: transform 0.3s ease;
  }

  .quantum-frame-corner-tl {
    top: 0;
    left: 0;
    border-right: none;
    border-bottom: none;
  }

  .quantum-frame-corner-tr {
    top: 0;
    right: 0;
    border-left: none;
    border-bottom: none;
  }

  .quantum-frame-corner-bl {
    bottom: 0;
    left: 0;
    border-right: none;
    border-top: none;
  }

  .quantum-frame-corner-br {
    bottom: 0;
    right: 0;
    border-left: none;
    border-top: none;
  }

  /* Quantum Scanning Effects */
  .quantum-scan-line {
    position: absolute;
    background: linear-gradient(
      90deg,
      transparent,
      var(--color-primary),
      transparent
    );
    opacity: 0.3;
    z-index: 3;
  }

  .quantum-scan-horizontal {
    height: 1px;
    width: 100%;
    top: 50%;
    left: 0;
    animation: quantum-scan-horizontal 4s linear infinite;
  }

  .quantum-scan-vertical {
    width: 1px;
    height: 100%;
    left: 50%;
    top: 0;
    animation: quantum-scan-vertical 4s linear infinite;
  }

  @keyframes quantum-scan-horizontal {
    0% {
      transform: translateY(-50vh);
    }
    100% {
      transform: translateY(50vh);
    }
  }

  @keyframes quantum-scan-vertical {
    0% {
      transform: translateX(-50vw);
    }
    100% {
      transform: translateX(50vw);
    }
  }

  /* Quantum Data Points */
  .quantum-data-points {
    position: absolute;
    inset: 0;
    z-index: 4;
  }

  .quantum-data-point {
    position: absolute;
    width: 8px;
    height: 8px;
    background: var(--color-primary);
    border-radius: 50%;
    top: var(--top);
    left: var(--left, auto);
    right: var(--right, auto);
    opacity: 0.7;
    animation: data-point-pulse calc(2s * var(--index)) ease-in-out infinite;
  }

  @keyframes data-point-pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.7;
    }
    50% {
      transform: scale(1.5);
      opacity: 1;
      box-shadow: 0 0 10px var(--color-primary);
    }
  }

  /* Quantum Hologram */
  .quantum-hologram {
    position: absolute;
    inset: 20px;
    z-index: 1;
    overflow: hidden;
    border-radius: 4px;
  }

  /* Quantum Projection Rings */
  .quantum-projection-rings {
    position: absolute;
    inset: -20px;
    z-index: 1;
    pointer-events: none;
  }

  .quantum-projection-ring {
    position: absolute;
    inset: 0;
    border: 1px solid var(--color-primary);
    border-radius: 50%;
    opacity: 0.3;
    animation: projection-ring-pulse 8s ease-in-out infinite;
  }

  @keyframes projection-ring-pulse {
    0% {
      transform: scale(0.8);
      opacity: 0.1;
    }
    50% {
      transform: scale(1.2);
      opacity: 0.3;
    }
    100% {
      transform: scale(0.8);
      opacity: 0.1;
    }
  }

  /* Quantum Profile Image */
  .quantum-profile-image {
    position: relative;
    width: 100%;
    height: 100%;
    border-radius: 4px;
    overflow: hidden;
    z-index: 2;
    transform-style: preserve-3d;
    transform: perspective(1000px);
    transition: transform 0.3s ease;
  }

  /* Quantum Overlay Effects */
  .quantum-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(99, 102, 241, 0.15),
      rgba(236, 72, 153, 0.15)
    );
    mix-blend-mode: overlay;
    z-index: 3;
  }

  .quantum-scanlines {
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      rgba(0, 0, 0, 0.1),
      rgba(0, 0, 0, 0.1) 1px,
      transparent 1px,
      transparent 4px
    );
    pointer-events: none;
    z-index: 4;
    opacity: 0.3;
  }

  /* Advanced Glitch Effects */
  .quantum-glitch-container {
    position: absolute;
    inset: 0;
    z-index: 5;
    pointer-events: none;
    overflow: hidden;
  }

  .quantum-glitch-layer {
    position: absolute;
    inset: 0;
    background-image: url("/images/about.png");
    background-size: cover;
    background-position: center;
    transform: translateX(var(--offset, 0));
    mix-blend-mode: var(--blend, normal);
    opacity: 0;
    animation: quantum-glitch 8s infinite;
  }

  @keyframes quantum-glitch {
    0%,
    100% {
      opacity: 0;
    }
    10.5% {
      opacity: 0.3;
    }
    10.6% {
      opacity: 0;
    }
    20.3% {
      opacity: 0.3;
    }
    20.4% {
      opacity: 0;
    }
    30.1% {
      opacity: 0.3;
    }
    30.2% {
      opacity: 0;
    }
    73.1% {
      opacity: 0.3;
    }
    73.3% {
      opacity: 0;
    }
  }

  /* Profile Image */
  .quantum-image-wrapper {
    position: relative;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .quantum-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  /* Quantum Particle Effects */
  .quantum-particles-overlay {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(
      circle at center,
      var(--color-primary) 1px,
      transparent 1px
    );
    background-size: 20px 20px;
    opacity: 0.1;
    mix-blend-mode: screen;
    z-index: 6;
    animation: particles-drift 20s linear infinite;
  }

  @keyframes particles-drift {
    0% {
      background-position: 0 0;
    }
    100% {
      background-position: 100px 100px;
    }
  }

  /* Quantum Energy Field */
  .quantum-energy-field {
    position: absolute;
    inset: -10px;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
    border-radius: 4px;
  }

  .quantum-energy-wave {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 1px solid var(--color-primary);
    opacity: 0.2;
    animation: energy-wave 6s ease-in-out infinite;
  }

  @keyframes energy-wave {
    0% {
      transform: scale(0);
      opacity: 0.5;
    }
    100% {
      transform: scale(2);
      opacity: 0;
    }
  }

  /* Quantum Floating Cards */
  .quantum-floating-card {
    position: absolute;
    z-index: 10;
    background: rgba(10, 10, 15, 0.9);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(10px);
    transform: translateY(0);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .quantum-floating-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
  }

  .quantum-tech-card {
    bottom: -20px;
    right: -20px;
    width: 240px;
    padding: 1rem;
  }

  .quantum-terminal-card {
    top: -20px;
    left: -20px;
    width: 330px;
  }

  .quantum-card-border {
    position: absolute;
    inset: 0;
    border: 1px solid var(--color-primary);
    border-radius: 8px;
    opacity: 0.5;
    z-index: -1;
  }

  .quantum-card-glow {
    position: absolute;
    inset: -5px;
    background: radial-gradient(
      circle at center,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.1;
    filter: blur(10px);
    z-index: -1;
  }

  .quantum-card-content {
    position: relative;
    z-index: 1;
  }

  /* Tech Icons */
  .quantum-tech-icons {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .quantum-tech-icon {
    position: relative;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    color: var(--color-primary);
  }

  .quantum-tech-icon-bg {
    position: absolute;
    inset: 0;
    background: rgba(99, 102, 241, 0.1);
    border-radius: 8px;
    z-index: -1;
  }

  .quantum-card-text {
    display: flex;
    flex-direction: column;
  }

  .quantum-card-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-text-alt);
    margin-bottom: 0.25rem;
  }

  .quantum-card-value {
    font-size: 0.9rem;
    font-weight: 600;
    color: white;
  }

  .quantum-card-decoration {
    position: absolute;
    bottom: 0.5rem;
    right: 0.5rem;
  }

  .quantum-card-dots {
    display: flex;
    gap: 4px;
  }

  .quantum-card-dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--color-primary);
    opacity: 0.7;
  }

  /* Quantum Terminal */
  .quantum-terminal-header {
    display: flex;
    align-items: center;
    padding: 0.5rem 1rem;
    background: rgba(99, 102, 241, 0.15);
    border-bottom: 1px solid rgba(99, 102, 241, 0.3);
  }

  .quantum-terminal-controls {
    display: flex;
    gap: 6px;
  }

  .quantum-terminal-control {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
  }

  .quantum-terminal-title {
    margin-left: auto;
    margin-right: auto;
    font-size: 0.7rem;
    color: var(--color-text-alt);
    letter-spacing: 1px;
  }

  .quantum-terminal-actions {
    display: flex;
    gap: 6px;
  }

  .quantum-terminal-action {
    width: 14px;
    height: 2px;
    background: rgba(255, 255, 255, 0.3);
    border-radius: 1px;
  }

  .quantum-terminal-content {
    padding: 1rem;
    font-family: "Courier New", monospace;
    font-size: 0.75rem;
    background: rgba(10, 10, 15, 0.95);
  }

  .quantum-terminal-line {
    display: flex;
    margin-bottom: 0.5rem;
    white-space: nowrap;
  }

  .quantum-terminal-prompt {
    color: var(--color-primary);
    margin-right: 0.5rem;
  }

  .quantum-terminal-command {
    color: rgba(255, 255, 255, 0.8);
  }

  .quantum-terminal-indent {
    padding-left: 1.5rem;
  }

  .quantum-terminal-var {
    color: #60a5fa;
  }

  .quantum-terminal-string {
    color: #10b981;
  }

  .quantum-terminal-func {
    color: #f59e0b;
  }

  .quantum-terminal-cursor {
    display: inline-block;
    width: 8px;
    height: 14px;
    background: var(--color-primary);
    vertical-align: middle;
    animation: cursor-blink 1s step-end infinite;
  }

  @keyframes cursor-blink {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0;
    }
  }

  .quantum-terminal-glow {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at center,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.05;
    filter: blur(10px);
    z-index: -1;
  }

  /* Quantum Content Container */
  .quantum-content-container {
    position: relative;
  }

  /* Quantum Section Badge */
  .quantum-section-badge {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .quantum-section-badge-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: rgba(99, 102, 241, 0.1);
    border-radius: 8px;
    color: var(--color-primary);
    font-size: 1.25rem;
    position: relative;
    overflow: hidden;
  }

  .quantum-section-badge-icon::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      45deg,
      transparent,
      rgba(99, 102, 241, 0.3),
      transparent
    );
    animation: badge-icon-shine 3s linear infinite;
  }

  @keyframes badge-icon-shine {
    0% {
      transform: translateX(-100%);
    }
    100% {
      transform: translateX(100%);
    }
  }

  .quantum-section-badge-text {
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 2px;
    color: var(--color-primary);
    text-transform: uppercase;
  }

  .quantum-section-badge-decoration {
    position: relative;
    margin-left: 0.5rem;
  }

  .quantum-badge-line {
    width: 30px;
    height: 1px;
    background: var(--color-primary);
    opacity: 0.7;
  }

  .quantum-badge-dot {
    position: absolute;
    top: 50%;
    right: 0;
    transform: translateY(-50%);
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--color-primary);
    animation: badge-dot-pulse 3s ease-in-out infinite;
  }

  @keyframes badge-dot-pulse {
    0%,
    100% {
      transform: translateY(-50%) scale(1);
      opacity: 0.7;
    }
    50% {
      transform: translateY(-50%) scale(1.5);
      opacity: 1;
    }
  }

  /* Quantum Section Title */
  .quantum-section-title {
    font-size: 2.25rem;
    font-weight: 700;
    line-height: 1.3;
    margin-bottom: 2rem;
    position: relative;
    display: inline-block;
  }

  .quantum-gradient-text {
    background: linear-gradient(
      90deg,
      var(--color-primary),
      var(--color-secondary)
    );
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradient-shift 8s linear infinite;
  }

  .quantum-title-decoration {
    position: relative;
    margin-top: 0.75rem;
  }

  .quantum-title-line {
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--color-primary), transparent);
  }

  .quantum-title-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 30px;
    height: 2px;
    background: var(--color-primary);
    filter: blur(2px);
    animation: title-glow-scan 3s linear infinite;
  }

  @keyframes title-glow-scan {
    0% {
      left: 0;
      opacity: 1;
    }
    50% {
      opacity: 0.5;
    }
    100% {
      left: 100%;
      opacity: 1;
    }
  }

  /* Quantum Text Container */
  .quantum-text-container {
    position: relative;
    margin-bottom: 2.5rem;
  }

  .quantum-text-interface {
    display: flex;
    gap: 1rem;
  }

  .quantum-text-line-numbers {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    padding-right: 1rem;
    border-right: 1px solid rgba(99, 102, 241, 0.3);
    font-family: "Courier New", monospace;
    font-size: 0.75rem;
    color: var(--color-text-alt);
    opacity: 0.7;
  }

  .quantum-line-number {
    line-height: 1.8;
    height: 1.8rem;
  }

  .quantum-text-content {
    flex: 1;
  }

  .quantum-paragraph {
    position: relative;
    line-height: 1.8;
    margin-bottom: 1.5rem;
    color: var(--color-text-alt);
  }

  .quantum-highlight {
    color: var(--color-primary);
    position: relative;
    display: inline-block;
    transition: color 0.3s ease;
  }

  .quantum-highlight::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--color-primary);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s ease;
  }

  .quantum-highlight:hover {
    color: var(--color-secondary);
  }

  .quantum-highlight:hover::after {
    transform: scaleX(1);
    transform-origin: left;
    background: var(--color-secondary);
  }

  .quantum-text-decoration {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .quantum-text-scan {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(
      90deg,
      transparent,
      var(--color-primary),
      transparent
    );
    opacity: 0.3;
    animation: text-scan 4s linear infinite;
  }

  @keyframes text-scan {
    0% {
      transform: translateY(-100%);
    }
    100% {
      transform: translateY(1000%);
    }
  }

  /* Quantum Stats */
  .quantum-stats-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }

  @media (min-width: 768px) {
    .quantum-stats-container {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .quantum-stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    position: relative;
    cursor: pointer;
    transition: transform 0.3s ease;
  }

  .quantum-stat-item:hover {
    transform: translateY(-5px);
  }

  .quantum-stat-item::after {
    content: "";
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%) scaleX(0);
    width: 60%;
    height: 2px;
    background: linear-gradient(
      90deg,
      var(--color-primary),
      var(--color-secondary)
    );
    transition: transform 0.3s ease;
  }

  .quantum-stat-item:hover::after {
    transform: translateX(-50%) scaleX(1);
  }

  .quantum-stat-hexagon {
    position: relative;
    width: 100px;
    height: 100px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .quantum-stat-item:hover .quantum-stat-hexagon {
    transform: rotate(30deg) scale(1.05);
  }

  .quantum-stat-hexagon-bg {
    position: absolute;
    inset: 0;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
    background: rgba(19, 19, 31, 0.5);
    z-index: -1;
    transition: background 0.3s ease;
  }

  .quantum-stat-item:hover .quantum-stat-hexagon-bg {
    background: rgba(99, 102, 241, 0.1);
  }

  .quantum-stat-hexagon-border {
    position: absolute;
    inset: 0;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
    background: transparent;
    border: 1px solid var(--color-primary);
    opacity: 0.5;
    z-index: 0;
    transition: all 0.3s ease;
  }

  .quantum-stat-item:hover .quantum-stat-hexagon-border {
    border-color: var(--color-secondary);
    opacity: 0.8;
    box-shadow: 0 0 15px rgba(99, 102, 241, 0.3);
  }

  .quantum-stat-value {
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(
      90deg,
      var(--color-primary),
      var(--color-secondary)
    );
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
    z-index: 2;
    transition: all 0.3s ease;
  }

  .quantum-stat-item:hover .quantum-stat-value {
    transform: rotate(-30deg) scale(1.1);
    background-position: right center;
    filter: drop-shadow(0 0 5px var(--color-primary));
  }

  .quantum-stat-progress {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    transition: all 0.3s ease;
  }

  .quantum-stat-item:hover .quantum-stat-progress {
    transform: rotate(30deg);
  }

  .quantum-stat-progress-path {
    fill: none;
    stroke: var(--color-primary);
    stroke-width: 2;
    stroke-dasharray: 320;
    stroke-dashoffset: calc(320 - (320 * var(--percent) / 100));
    opacity: 0.3;
    filter: drop-shadow(0 0 5px var(--color-primary));
    transition: all 0.5s ease-in-out;
  }

  .quantum-stat-item:hover .quantum-stat-progress-path {
    stroke: var(--color-secondary);
    opacity: 0.6;
    filter: drop-shadow(0 0 10px var(--color-secondary));
    stroke-width: 3;
  }

  .quantum-stat-label {
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-text-alt);
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .quantum-stat-item:hover .quantum-stat-label {
    color: white;
    letter-spacing: 2px;
  }

  /* Stat Tooltip */
  .quantum-stat-tooltip {
    position: absolute;
    top: -60px;
    left: 50%;
    transform: translateX(-50%) scale(0);
    background: rgba(19, 19, 31, 0.9);
    border: 1px solid var(--color-primary);
    border-radius: 4px;
    padding: 0.5rem 1rem;
    font-size: 0.75rem;
    color: white;
    white-space: nowrap;
    opacity: 0;
    z-index: 10;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .quantum-stat-tooltip::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 50%;
    transform: translateX(-50%) rotate(45deg);
    width: 10px;
    height: 10px;
    background: rgba(19, 19, 31, 0.9);
    border-right: 1px solid var(--color-primary);
    border-bottom: 1px solid var(--color-primary);
  }

  .quantum-stat-item:hover .quantum-stat-tooltip {
    transform: translateX(-50%) scale(1);
    opacity: 1;
  }

  /* Quantum Service Cards */
  .quantum-services-container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 768px) {
    .quantum-services-container {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .quantum-service-card {
    position: relative;
    background: rgba(19, 19, 31, 0.7);
    border: 1px solid var(--color-primary);
    border-radius: 8px;
    padding: 1.5rem;
    overflow: hidden;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .quantum-service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(99, 102, 241, 0.2);
    border-color: var(--color-secondary);
  }

  .quantum-service-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    position: relative;
  }

  .quantum-service-icon-container {
    position: relative;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .quantum-service-icon-bg {
    position: absolute;
    inset: 0;
    background: rgba(99, 102, 241, 0.1);
    border-radius: 8px;
    z-index: 0;
  }

  .quantum-service-icon {
    font-size: 1.5rem;
    color: var(--color-primary);
    position: relative;
    z-index: 1;
    transition: all 0.3s ease;
  }

  .quantum-service-card:hover .quantum-service-icon {
    transform: scale(1.1);
    color: var(--color-secondary);
  }

  .quantum-service-icon-glow {
    position: absolute;
    inset: -5px;
    background: radial-gradient(
      circle at center,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.1;
    filter: blur(5px);
    z-index: 0;
    transition: all 0.3s ease;
  }

  .quantum-service-card:hover .quantum-service-icon-glow {
    opacity: 0.2;
    background: radial-gradient(
      circle at center,
      var(--color-secondary) 0%,
      transparent 70%
    );
  }

  .quantum-service-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: white;
    flex: 1;
  }

  .quantum-service-tag {
    font-size: 0.75rem;
    color: var(--color-primary);
    opacity: 0.7;
    font-family: monospace;
    transition: color 0.3s ease;
  }

  .quantum-service-card:hover .quantum-service-tag {
    color: var(--color-secondary);
  }

  .quantum-service-content {
    padding-left: 1rem;
    border-left: 1px solid rgba(99, 102, 241, 0.3);
  }

  .quantum-service-description {
    font-size: 0.9rem;
    color: var(--color-text-alt);
    line-height: 1.6;
  }

  .quantum-service-decoration {
    position: relative;
    margin-top: 1rem;
    padding-top: 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .quantum-service-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, var(--color-primary), transparent);
    opacity: 0.3;
  }

  .quantum-service-dots {
    display: flex;
    gap: 4px;
  }

  .quantum-service-dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--color-primary);
    opacity: 0.7;
    transition: background 0.3s ease;
  }

  .quantum-service-card:hover .quantum-service-dot {
    background: var(--color-secondary);
  }

  .quantum-service-glow {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at center,
      var(--color-primary) 0%,
      transparent 70%
    );
    opacity: 0.05;
    filter: blur(10px);
    z-index: -1;
    transition: all 0.3s ease;
  }

  .quantum-service-card:hover .quantum-service-glow {
    opacity: 0.1;
    background: radial-gradient(
      circle at center,
      var(--color-secondary) 0%,
      transparent 70%
    );
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    // Initialize quantum particle system
    function initQuantumParticles() {
      const particleContainer = document.getElementById("quantum-particles");
      if (!particleContainer) return;

      // Create particles
      for (let i = 0; i < 50; i++) {
        const particle = document.createElement("div");
        particle.className = "quantum-particle";

        // Random position
        const posX = Math.random() * 100;
        const posY = Math.random() * 100;

        // Random size
        const size = Math.random() * 3 + 1;

        // Random opacity
        const opacity = Math.random() * 0.5 + 0.1;

        // Random animation duration
        const duration = Math.random() * 20 + 10;

        // Apply styles
        particle.style.cssText = `
          left: ${posX}%;
          top: ${posY}%;
          width: ${size}px;
          height: ${size}px;
          opacity: ${opacity};
          animation-duration: ${duration}s;
          animation-delay: ${Math.random() * 5}s;
        `;

        particleContainer.appendChild(particle);
      }
    }

    // Initialize quantum counter animations
    function animateQuantumCounters() {
      const counters = document.querySelectorAll(".counter");

      counters.forEach((counter) => {
        const target = parseInt(counter.getAttribute("data-target"), 10);
        const duration = 2000; // 2 seconds
        const step = target / (duration / 16); // 60fps

        let current = 0;
        const timer = setInterval(() => {
          current += step;
          counter.textContent = Math.round(current);

          // Add quantum glitch effect randomly during counting
          if (Math.random() < 0.1) {
            counter.style.textShadow =
              "0 0 10px var(--color-primary), 0 0 20px var(--color-primary)";
            setTimeout(() => {
              counter.style.textShadow = "";
            }, 100);
          }

          if (current >= target) {
            counter.textContent = target;
            clearInterval(timer);
          }
        }, 16);
      });
    }

    // Initialize counter animations when section is in view
    const aboutSection = document.getElementById("about");

    if (aboutSection) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              animateQuantumCounters();
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.2 }
      );

      observer.observe(aboutSection);
    }

    // Initialize VanillaTilt for the quantum profile image
    if (typeof VanillaTilt !== "undefined") {
      const tiltElements = document.querySelectorAll(".tilt");
      if (tiltElements.length > 0) {
        VanillaTilt.init(tiltElements, {
          max: 10,
          speed: 400,
          glare: true,
          "max-glare": 0.5,
          gyroscope: true,
          scale: 1.05,
          perspective: 1000,
        });
      }
    }

    // Quantum glitch effects
    function initQuantumGlitchEffects() {
      // Random glitch effect for quantum elements
      setInterval(() => {
        // Glitch layers
        const glitchLayers = document.querySelectorAll(".quantum-glitch-layer");
        if (glitchLayers.length > 0 && Math.random() < 0.3) {
          const randomLayer =
            glitchLayers[Math.floor(Math.random() * glitchLayers.length)];
          randomLayer.style.opacity = "0.3";

          setTimeout(() => {
            randomLayer.style.opacity = "0";
          }, 100);
        }

        // Random displacement of quantum frame corners
        const corners = document.querySelectorAll(".quantum-frame-corner");
        if (corners.length > 0 && Math.random() < 0.2) {
          const randomCorner =
            corners[Math.floor(Math.random() * corners.length)];
          const randomX = (Math.random() - 0.5) * 8;
          const randomY = (Math.random() - 0.5) * 8;

          randomCorner.style.transform = `translate(${randomX}px, ${randomY}px)`;

          setTimeout(() => {
            randomCorner.style.transform = "";
          }, 200);
        }

        // Random data point pulse
        const dataPoints = document.querySelectorAll(".quantum-data-point");
        if (dataPoints.length > 0 && Math.random() < 0.3) {
          const randomPoint =
            dataPoints[Math.floor(Math.random() * dataPoints.length)];

          randomPoint.style.transform = "scale(1.5)";
          randomPoint.style.boxShadow = "0 0 10px var(--color-primary)";

          setTimeout(() => {
            randomPoint.style.transform = "";
            randomPoint.style.boxShadow = "";
          }, 300);
        }
      }, 2000);
    }

    // Initialize stat item hover effects
    function initStatItemEffects() {
      const statItems = document.querySelectorAll(".quantum-stat-item");

      statItems.forEach((item) => {
        // Add particle burst effect on hover
        item.addEventListener("mouseenter", () => {
          // Create particle burst
          createParticleBurst(item);

          // Play hover sound effect (subtle)
          playHoverSound();
        });
      });
    }

    // Create particle burst effect
    function createParticleBurst(element) {
      const rect = element.getBoundingClientRect();
      const centerX = rect.left + rect.width / 2;
      const centerY = rect.top + rect.height / 2;

      // Create particles
      for (let i = 0; i < 12; i++) {
        const particle = document.createElement("div");
        particle.className = "quantum-burst-particle";

        // Random angle and distance
        const angle = Math.random() * Math.PI * 2;
        const distance = Math.random() * 50 + 20;

        // Calculate position
        const x = centerX + Math.cos(angle) * distance;
        const y = centerY + Math.sin(angle) * distance;

        // Random size
        const size = Math.random() * 6 + 2;

        // Random color
        const colors = [
          "var(--color-primary)",
          "var(--color-secondary)",
          "var(--color-accent)",
        ];
        const color = colors[Math.floor(Math.random() * colors.length)];

        // Apply styles
        particle.style.cssText = `
          position: fixed;
          left: ${centerX}px;
          top: ${centerY}px;
          width: ${size}px;
          height: ${size}px;
          background: ${color};
          border-radius: 50%;
          pointer-events: none;
          z-index: 9999;
          opacity: 1;
          transform: translate(-50%, -50%);
        `;

        document.body.appendChild(particle);

        // Animate particle
        const keyframes = [
          {
            transform: "translate(-50%, -50%) scale(0)",
            opacity: 1,
          },
          {
            transform: `translate(calc(${x - centerX}px - 50%), calc(${
              y - centerY
            }px - 50%)) scale(1)`,
            opacity: 0.8,
          },
          {
            transform: `translate(calc(${x - centerX}px - 50%), calc(${
              y - centerY
            }px - 50%)) scale(0)`,
            opacity: 0,
          },
        ];

        const timing = {
          duration: Math.random() * 1000 + 500,
          easing: "cubic-bezier(0.4, 0, 0.2, 1)",
        };

        const animation = particle.animate(keyframes, timing);

        animation.onfinish = () => {
          particle.remove();
        };
      }
    }

    // Play subtle hover sound
    function playHoverSound() {
      // Check if Web Audio API is supported
      if (
        typeof AudioContext !== "undefined" ||
        typeof webkitAudioContext !== "undefined"
      ) {
        try {
          const AudioContextClass =
            window.AudioContext || window.webkitAudioContext;
          const audioCtx = new AudioContextClass();

          // Create oscillator
          const oscillator = audioCtx.createOscillator();
          const gainNode = audioCtx.createGain();

          // Set oscillator type and frequency
          oscillator.type = "sine";
          oscillator.frequency.setValueAtTime(1200, audioCtx.currentTime);
          oscillator.frequency.exponentialRampToValueAtTime(
            600,
            audioCtx.currentTime + 0.2
          );

          // Set volume (very quiet)
          gainNode.gain.setValueAtTime(0.05, audioCtx.currentTime);
          gainNode.gain.exponentialRampToValueAtTime(
            0.001,
            audioCtx.currentTime + 0.2
          );

          // Connect nodes
          oscillator.connect(gainNode);
          gainNode.connect(audioCtx.destination);

          // Start and stop
          oscillator.start();
          oscillator.stop(audioCtx.currentTime + 0.2);
        } catch (e) {
          console.log("Audio context error:", e);
        }
      }
    }

    // Initialize all effects
    initQuantumParticles();
    initQuantumGlitchEffects();
    initStatItemEffects();
  });
</script>
